# Fix 2026-02-16

- Added `VITE_API_URL` and centralized API base URL logic.
- Replaced hardcoded `http://localhost:8001` with `apiUrl(...)` in auth + upload pages.
- Added shared auth header helper for API calls.
- Removed Supabase auth context, client, migrations, and env variables.
- Removed `@supabase/supabase-js` from `package.json`.
- Refreshed `package-lock.json` via `npm install` after removing Supabase.
- Deleted leftover Supabase directories.
- Fixed Admin Settings crash by removing undefined user references.
- Added redirect placeholder screens to admin pages and profile.
- Fixed notifications unread badge by seeding demo notifications.
- Replaced em dashes and emoji icons with ASCII to avoid encoding issues.
- Removed the navbar search UI for now.
- Made admin layout responsive with stacked sidebar on small screens.
- Wired Admin Users page to `/admin/users` and added loading + error handling.
- Replaced demo user actions with real View/Edit/Delete using admin APIs.
- Wired My Wardrobe to backend wardrobe endpoints with image upload, list, and delete.
- Moved wardrobe upload to a modal and added category + description fields.
- Ensured pages with footers use flex layout so the footer stays at the bottom.
- Profile now fetches name from backend, updates via `/account`, and shows initials avatar when no photo is set.
- Added a user sidebar for dashboard navigation and simplified profile layout.
- Switched user sidebar to a right-side slide-out drawer with toggle button.
- Replaced dashboard navbar action with a 3-dot menu toggle and moved dashboard access into the drawer.
- Removed profile icon from the top navbar and anchored the drawer below the navbar.
- Restored Navbar on Profile and Dashboard pages alongside the drawer.
