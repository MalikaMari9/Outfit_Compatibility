# Fix 2026-02-16

- Added `start` and `dev` scripts to `backend/package.json`.
- Fixed password verification to `await compare(...)` in login.
- Defaulted signup role to `user` when missing.
- Ensured JWT verification uses `JWT_SECRET` and handled invalid tokens safely in controllers.
- Corrected Mongoose schema validation (`required` instead of `require`).
- Fixed clothing model reference to `ref: "user"`.
- Fixed CORS config to use `origin` and aligned `CLIENT_HOST` to `http://localhost:5173`.
- Added stub endpoints for `/recommend`, `/compatibility`, and `/feedback` returning 501.
- Allowed multiple CORS origins via comma-separated `CLIENT_HOST` list.
- Added `nodemon` and updated `npm run dev` to auto-restart the backend.
- Added root `.gitignore` to ignore `.env` files.
- Added startup validation to fail fast when `DB_LINK`, `HTTP_PORT`, or `JWT_SECRET` are missing.
- Locked signup role to `user` so clients cannot self-assign admin.
- Added admin-only `/admin/users` endpoint with pagination (default limit 6).
- Added admin user CRUD endpoints (`GET`, `PATCH`, `DELETE /admin/users/:id`) with role enforcement.
- Added wardrobe CRUD (list/add/delete) with image uploads and static `/uploads` serving.
- Added wardrobe category (top/bottom) validation and optional description field.
