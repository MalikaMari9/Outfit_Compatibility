# Fix Log - 2026-02-23

## Scope
Consolidate outfit pipeline assets into `OutfitCompatibility`, remove external folder dependency at runtime, and capture integration decisions before backend wiring.

## Applied Fixes
1. Self-contained structure created:
- `services/pipeline`
- `services/llm`
- `assets/models`
- `assets/data`
- `runtime/cache`
- `runtime/uploads`
- `docs`

2. Pipeline code migrated into repository:
- copied `src`, `scripts`, `configs`, `requirements.txt`, and docs into `services/pipeline`.

3. Ollama helper code migrated into repository:
- copied `chat_rag.py`, `index_kb.py`, `README.md`, and `kb/` into `services/llm`.

4. Required model files copied into `assets/models`:
- `compat_top_bottom.pt`
- `A_best_pattern_clean_colab.pt`
- `B_best_category_tempered.pt`
- `B_class_mapping_tempered.csv`
- `yolov8n-pose.pt`

5. Polyvore data copied into `assets/data/polyvore_outfits`.

6. Pipeline config paths updated to repo-local paths in:
- `services/pipeline/configs/pipeline_config.json`

7. Fallback default paths updated to repo-local values in:
- `services/pipeline/src/outfit_pipeline/config.py`

8. Repository ignore rules hardened in:
- `.gitignore`
- added runtime/cache/output ignores, Python cache ignores, local LLM artifacts, and node_modules ignores.

9. Migration documentation added:
- `docs/SELF_CONTAINED_MIGRATION_2026-02-23.md`

## Validation Results
1. Script entry checks:
- `python services/pipeline/scripts/run_pair.py --help` passed
- `python services/pipeline/scripts/run_rank.py --help` passed

2. Functional smoke checks:
- dataset-image pair inference passed with local assets/data
- non-dataset image inference passed with category fallback and autocrop flow

3. Path audit:
- executable pipeline code no longer references external sibling folders.

## Integration Findings (Before Wiring)
1. Backend ML endpoints still stubbed:
- `backend/route.js` returns `501` for `/recommend`, `/compatibility`, `/feedback`.

2. Backend currently enforces required env on startup:
- `DB_LINK`, `HTTP_PORT`, `JWT_SECRET` in `backend/config.js`.

3. Upload path behavior requires unification during wiring:
- current multer/static path handling still centered around `backend/uploads`.

4. Git readiness risks:
- `backend/.env` contains real credentials and must be replaced with `.env.example` before git push.
- large model files require Git LFS policy before publishing.

## Policy Decisions Confirmed
1. Keep MongoDB as required backend startup dependency (current policy).
2. Use a single upload root (`runtime/uploads`) for consistency.
3. Keep Ollama enabled.
4. Add loading state UX in frontend during long-running analysis/explanation calls.

## Deferred for Future
1. Hybrid availability mode documented in:
- `docs/futureImplementation.md`

2. Backend wiring implementation of:
- `/compatibility`
- `/recommend`
- `/feedback`
Status: completed in "Wiring Pass Update - 2026-02-23" below.

3. Mongo collection addition for user reports (recommended):
- `feedback_reports`.

## Wiring Pass Update - 2026-02-23
1. Backend endpoint wiring completed:
- replaced `501` stubs in `backend/route.js` with real handlers for:
  - `POST /compatibility`
  - `POST /recommend`
  - `POST /feedback`

2. Inference controller and pipeline bridge added:
- new controller: `backend/controllers/inference.js`
- new utility: `backend/utils/pipeline.js`
- backend now executes:
  - `services/pipeline/scripts/run_pair.py`
  - `services/pipeline/scripts/run_recommend.py`
- response uses pipeline JSON output with `--public-output`.
- bridge now also cleans temporary backend JSON output artifacts even on failed pipeline runs.

3. Single upload-root policy applied:
- added path resolver `backend/utils/paths.js`
- upload middleware rewritten in `backend/utils/img.js`
- root is now `runtime/uploads` with:
  - `runtime/uploads/persistent` (wardrobe)
  - `runtime/uploads/temp` (inference temp files)
- temp uploaded files are cleaned up after inference request completion.

4. Static upload serving updated:
- `backend/app.js` now serves `/uploads` from `runtime/uploads/persistent`.

5. Feedback persistence added (Mongo):
- new model: `backend/models/feedbackReport.js`
- new controller: `backend/controllers/feedback.js`
- `/feedback` now writes report records with optional user link, page, message, and status.

6. Recommend auto-detection script added:
- new script: `services/pipeline/scripts/run_recommend.py`
- supports `--mode auto` and auto-detects semantic (`tops`/`bottoms`) from category inference.
- falls back to `top2bottom` when semantic cannot be determined.

7. Frontend loading-stage UX added:
- `frontend/wardo-hub-main/src/pages/MixMatch.tsx`
- `frontend/wardo-hub-main/src/pages/GlowUp.tsx`
- added staged loading text + progress bar while request is in-flight.

8. Validation run for this pass:
- Node syntax checks passed for all changed backend JS files.
- `python services/pipeline/scripts/run_recommend.py --help` passed.
- full recommend smoke test passed (`--public-output`) using local upload sample.
- frontend type-check passed:
  - `cd frontend/wardo-hub-main; npx tsc -p tsconfig.json --noEmit`

## Verification Pass - 2026-02-23 (Live API)
1. Backend startup and health:
- `GET /health` returned `200` with `{ \"status\": \"ok\" }`.
- `GET /health/ollama` returned `200`, Ollama host reachable, models listed.

2. Feedback flow:
- `POST /feedback` returned success with persisted report id and `status: pending`.

3. Compatibility inference:
- `POST /compatibility` with multipart `top` + `bottom` returned valid JSON.
- Response included label/final score and `details.llm_status = ok`.

4. Recommendation inference:
- `POST /recommend` with single image returned valid JSON.
- Auto-detection selected mode (`top2bottom`) with semantic detection metadata.
- Returned top-k results with rank-1 explanation status available.

5. Wardrobe + static file serving:
- Signup + authenticated wardrobe upload succeeded.
- Uploaded image stored under `runtime/uploads/persistent`.
- Direct fetch of `/uploads/<filename>` verified with `HTTP 200`.
- Note: an earlier PowerShell `Invoke-WebRequest` check produced a null-reference message; re-check with `curl` confirmed static serving is correct.

## Debug Console Pass - 2026-02-23
1. Backend debug tracing added for inference endpoints:
- updated `backend/controllers/inference.js` to support `debug=1` request flag.
- compatibility and recommend now append structured stage logs:
  - `request_received`
  - `input_validation` / `input_error`
  - `pipeline_start`
  - `pipeline_success` / `pipeline_error`
  - `cleanup`
- when enabled, trace is attached to response under `details.debug_trace` (or top-level fallback).

2. MixMatch debug popup implemented:
- updated `frontend/wardo-hub-main/src/pages/MixMatch.tsx`.
- added `Debug Console` button and popup dialog.
- popup displays:
  - real-time frontend lifecycle logs
  - backend debug trace entries from response
  - structured metadata/error payloads when present
- added toggle to include/exclude backend trace (`debug=1` form field).

3. Validation for debug flow:
- frontend type-check passed:
  - `cd frontend/wardo-hub-main; npx tsc -p tsconfig.json --noEmit`
- live compatibility request with `debug=1` returned trace successfully:
  - `debug_trace_count = 4`
  - first stage = `request_received`

## Loading Overlay UX Pass - 2026-02-23
1. Replaced inline loading bars with blocking overlays:
- updated `frontend/wardo-hub-main/src/pages/MixMatch.tsx`
- updated `frontend/wardo-hub-main/src/pages/GlowUp.tsx`
- behavior now uses a centered block overlay with:
  - 3-dot animation
  - existing stage text and percentage progress
  - background blur + interaction block while request is in-flight

2. Added rotating fashion tips (hardcoded frontend list):
- each page now cycles style tips every ~2.8 seconds while loading
- no Mongo dependency added for tips in this pass

3. Validation for this pass:
- frontend type-check passed:
  - `cd frontend/wardo-hub-main; npx tsc -p tsconfig.json --noEmit`

## Ollama Latency Reduction Pass - 2026-02-23
1. Reduced generation cost by default:
- updated `services/pipeline/configs/pipeline_config.json`:
  - `temperature: 0.1` (was `0.2`)
  - `max_tokens: 160` (was `0`, unlimited/default)
  - `retries: 0` (was `1`)

2. Hardened low-latency defaults in config loader:
- updated `services/pipeline/src/outfit_pipeline/config.py` defaults to align with low-latency values:
  - `temperature=0.1`, `max_tokens=160`, `retries=0`

3. Improved Ollama cache hit rate for API uploads:
- updated `services/pipeline/src/outfit_pipeline/ollama_explainer.py`
- cache key now normalizes facts and ignores volatile image-path fields (`top_image`, `bottom_image`, `query_image`, `candidate_image`)
- this avoids cache misses caused by per-request temp upload filenames

4. Reduced prompt/runtime overhead in Ollama adapter:
- normalized facts are also used for prompt payload (shorter, stable facts)
- generate options now include:
  - `num_predict` default cap when not explicitly set
  - `num_ctx=1024`
  - `keep_alive=\"30m\"`

5. Validation for this pass:
- `python services/pipeline/scripts/run_pair.py --help` passed
- `python services/pipeline/scripts/run_rank.py --help` passed
- `python services/pipeline/scripts/run_recommend.py --help` passed

## Loading Debug Timer Pass - 2026-02-23
1. Added live elapsed timer on blocking loading overlays:
- `frontend/wardo-hub-main/src/pages/MixMatch.tsx`
- `frontend/wardo-hub-main/src/pages/GlowUp.tsx`

2. Timer behavior:
- starts when request enters loading state
- updates every 100ms
- resets when loading completes
- displayed as `mm:ss.t` in overlay under stage/progress as:
  - `Debug timer: 00:12.3`

3. Validation for this pass:
- frontend type-check passed:
  - `cd frontend/wardo-hub-main; npx tsc -p tsconfig.json --noEmit`

## Qwen + Async Ollama Pass - 2026-02-23
1. Switched default explanation model to Qwen 2.5 (1.5B):
- `services/pipeline/configs/pipeline_config.json`
- `services/pipeline/src/outfit_pipeline/config.py`
- model now defaults to `qwen2.5:1.5b`

2. Added defer-LLM mode in pipeline engine:
- updated `services/pipeline/src/outfit_pipeline/engine.py`
- `score_pair(...)` and `rank(...)` now accept `include_llm` (default `true`)
- when disabled, response marks `llm_status: deferred` instead of blocking on Ollama

3. Added CLI support to skip synchronous explanation:
- updated scripts:
  - `services/pipeline/scripts/run_pair.py`
  - `services/pipeline/scripts/run_rank.py`
  - `services/pipeline/scripts/run_recommend.py`
- new flag: `--defer-llm`

4. Added dedicated explanation script:
- new file: `services/pipeline/scripts/run_explain.py`
- consumes prepared `facts` JSON and returns:
  - `llm_status`
  - `llm_cached`
  - optional `llm_explanation`, `llm_raw`, `llm_error`

5. Backend async explanation API added:
- updated `backend/utils/pipeline.js`:
  - added `runOllamaExplanation(...)`
  - added `deferLlm` support in pair/recommend runner helpers
- updated `backend/controllers/inference.js`:
  - compatibility/recommend now run with deferred LLM (`deferLlm: true`)
  - new handler `explainFromFacts`
- updated `backend/route.js`:
  - new endpoint `POST /explain`

6. Frontend now resolves explanation in second step (non-blocking):
- updated `frontend/wardo-hub-main/src/pages/MixMatch.tsx`
  - calls `/compatibility` first (fast)
  - then calls `/explain` with compact facts and merges LLM fields into result JSON
  - debug console logs async explanation lifecycle
- updated `frontend/wardo-hub-main/src/pages/GlowUp.tsx`
  - calls `/recommend` first (fast)
  - then calls `/explain` for rank-1 candidate facts and merges LLM fields into rank-1 details

7. Validation for this pass:
- `python services/pipeline/scripts/run_pair.py --help` passed
- `python services/pipeline/scripts/run_rank.py --help` passed
- `python services/pipeline/scripts/run_recommend.py --help` passed
- `python services/pipeline/scripts/run_explain.py --help` passed
- backend JS syntax checks passed:
  - `node --check backend/utils/pipeline.js`
  - `node --check backend/controllers/inference.js`
  - `node --check backend/route.js`
- frontend type-check passed:
  - `cd frontend/wardo-hub-main; npx tsc -p tsconfig.json --noEmit`

## Async Explain Bugfix - 2026-02-23
1. Fixed `/explain` 500 caused by missing facts JSON file:
- updated `backend/utils/pipeline.js`
- root cause:
  - `runOllamaExplanation(...)` returned `runPipelineScript(...)` without `await`
  - `finally` cleanup ran immediately and deleted facts file before Python could read it
- fix:
  - changed to `return await runPipelineScript(...)` so facts cleanup runs only after script completes

2. Validation for this fix:
- backend syntax checks passed:
  - `node --check backend/utils/pipeline.js`
  - `node --check backend/controllers/inference.js`
- explain script help check passed:
  - `python services/pipeline/scripts/run_explain.py --help`

## Public Response + Friendly UI Pass - 2026-02-23
1. Sanitized backend output for user-facing endpoints:
- updated `backend/controllers/inference.js`
- removed internal fields from normal responses:
  - `details.debug_trace` (unless debug explicitly requested)
  - `details.llm_raw`, `details.llm_error` (unless debug explicitly requested)
  - `debug_trace` top-level fallback (unless debug explicitly requested)
  - `processed_path` under autocrop blocks (internal runtime path)

2. Sanitized `/explain` response for frontend consumption:
- updated `backend/controllers/inference.js` (`explainFromFacts`)
- now returns a public payload shape by default:
  - `llm_status`
  - `llm_cached`
  - optional `llm_explanation`
- non-public internals (`llm_raw`, `llm_error`) are omitted unless debug is explicitly requested.

3. MixMatch UI changed from raw JSON dump to structured user view:
- updated `frontend/wardo-hub-main/src/pages/MixMatch.tsx`
- replaced "Server Response" block with:
  - compatibility summary card (label, score, detected categories/colors)
  - component score bars (`model`, `type`, `color`, `brightness`, `pattern`)
  - explanation card with status badge and readable sections
- debug mode is now opt-in by default (`debugEnabled=false`), and debug popup no longer auto-opens on analyze.

4. GlowUp UI changed from raw JSON dump to structured user view:
- updated `frontend/wardo-hub-main/src/pages/GlowUp.tsx`
- replaced "Server Response" block with:
  - recommendation summary card (score, label, categories/colors)
  - component score bars
  - explanation card with status badge and readable sections
- deferred explanation merge now keeps public fields only.

5. Validation for this pass:
- backend syntax checks passed:
  - `node --check backend/controllers/inference.js`
  - `node --check backend/route.js`
  - `node --check backend/utils/pipeline.js`
- frontend type-check passed:
  - `cd frontend/wardo-hub-main; npx tsc -p tsconfig.json --noEmit`

## Explain Fallback Reliability Pass - 2026-02-23
1. Added deterministic explanation fallback when Ollama JSON is invalid:
- updated `services/pipeline/scripts/run_explain.py`
- if `llm_explanation` is missing (e.g., `invalid_json`), script now generates a structured fallback explanation from facts:
  - `summary`
  - `why_it_works`
  - `risk_points`
  - `style_suggestion`
  - `confidence_note`
  - `disclaimer`
- effective public status is returned as `llm_status: ok` so frontend does not show `Unavailable`.

2. Adjusted explain payload sanitization behavior:
- updated `backend/controllers/inference.js`
- when an explanation object exists, public `llm_status` is normalized to `ok`.

3. Validation for this pass:
- `python services/pipeline/scripts/run_explain.py --help` passed
- `node --check backend/controllers/inference.js` passed

## Stylist Tone Upgrade Pass - 2026-02-23
1. Upgraded Ollama style prompt for higher-quality language:
- updated `services/pipeline/src/outfit_pipeline/ollama_explainer.py`
- prompt now explicitly requires:
  - stylist tone (not robotic)
  - concrete, actionable advice
  - concise structured bullets
- prompt version bumped from `v1` to `v2` to invalidate old cached explanations.

2. Upgraded fallback explanation writer to sound less mechanical:
- updated `services/pipeline/scripts/run_explain.py`
- fallback now derives:
  - palette relation (`monochrome`, `neutral-anchor`, `analogous`, `contrast`)
  - outfit vibe naming (e.g., clean polished minimal, balanced casual chic)
  - more specific risk language
  - stronger styling suggestions (accessory/shape/layering moves)
- output schema remains unchanged for frontend compatibility.

3. Validation for this pass:
- `python -m py_compile services/pipeline/src/outfit_pipeline/ollama_explainer.py services/pipeline/scripts/run_explain.py` passed

## Humanization + Source Transparency Pass - 2026-02-23
1. Added multi-stage explanation generation to reduce robotic output:
- updated `services/pipeline/src/outfit_pipeline/ollama_explainer.py`
- new flow:
  - strict JSON Ollama pass
  - one extra strict-JSON retry for parse failures
  - non-JSON stylist text pass with section parsing back into schema
  - final local deterministic fallback only if model passes fail
- strict JSON pass now uses a slightly higher token cap (`>=200`) to reduce truncation/invalid JSON.

2. Added explicit explanation source metadata:
- updated `services/pipeline/scripts/run_explain.py`
- response now includes `llm_source`:
  - `ollama_json`, `ollama_json_retry`, `ollama_text`, `ollama_cache`, or `fallback`
- fallback responses now use `llm_status: fallback` (instead of pretending `ok`).

3. Public response sanitization aligned with new statuses:
- updated `backend/controllers/inference.js`
- `/explain` now returns:
  - `llm_status`: `ok`, `fallback`, `deferred`, or `unavailable`
  - `llm_source`
  - `llm_cached`
  - optional `llm_explanation`

4. Frontend badges now distinguish real stylist output vs quick fallback:
- updated `frontend/wardo-hub-main/src/pages/MixMatch.tsx`
- updated `frontend/wardo-hub-main/src/pages/GlowUp.tsx`
- badge behavior:
  - `AI Stylist` for `llm_status=ok`
  - `Quick Analysis` for `llm_status=fallback`
  - `Generating...` for deferred
  - `Unavailable` otherwise

5. Validation for this pass:
- `python -m py_compile services/pipeline/src/outfit_pipeline/ollama_explainer.py services/pipeline/scripts/run_explain.py` passed
- `python services/pipeline/scripts/run_explain.py --help` passed
- `node --check backend/controllers/inference.js` passed
- frontend type-check passed:
  - `cd frontend/wardo-hub-main; npx tsc -p tsconfig.json --noEmit`

## Anti-Robotic Explanation Pass - 2026-02-23
1. Added robotic-output detection and rewrite:
- updated `services/pipeline/scripts/run_explain.py`
- new behavior:
  - normalize explanation shape from LLM outputs
  - detect robotic phrasing patterns (e.g., `component`, `final score`, list-like raw strings)
  - rewrite into human stylist wording based on facts (silhouette, palette relation, depth, texture)
  - preserve schema while making tone more natural
- rewritten outputs are marked in source metadata as `*_rewritten`.

2. Fixed suggestion shape issues in frontend:
- updated `frontend/wardo-hub-main/src/pages/MixMatch.tsx`
- updated `frontend/wardo-hub-main/src/pages/GlowUp.tsx`
- `style_suggestion` now supports both string and array payloads cleanly:
  - single sentence -> paragraph
  - multiple suggestions -> bullet list

3. Validation for this pass:
- `python -m py_compile services/pipeline/scripts/run_explain.py services/pipeline/src/outfit_pipeline/ollama_explainer.py` passed
- `python services/pipeline/scripts/run_explain.py --help` passed
- `node --check backend/controllers/inference.js` passed
- frontend type-check passed:
  - `cd frontend/wardo-hub-main; npx tsc -p tsconfig.json --noEmit`

## Pattern Type Frontend Pass - 2026-02-23
1. Added explicit pattern type display in MixMatch and GlowUp results:
- updated `frontend/wardo-hub-main/src/pages/MixMatch.tsx`
- updated `frontend/wardo-hub-main/src/pages/GlowUp.tsx`
- new user-facing fields:
  - `Top Pattern` (e.g., Solid, Floral, Striped)
  - `Bottom Pattern`
  - confidence text for detected patterned items
  - `Pattern pairing` summary line

2. Passed pattern metadata into explanation facts payload:
- updated both pages' facts builders (`buildPairFacts`, `buildRecommendFacts`)
- added:
  - `top_pattern_name`, `bottom_pattern_name`
  - `top_pattern_prob`, `bottom_pattern_prob`

3. Explanation writer now uses pattern names when available:
- updated `services/pipeline/scripts/run_explain.py`
- fallback and human-rewrite paths now reference pattern pairing in `why_it_works`.

4. Validation for this pass:
- `python -m py_compile services/pipeline/scripts/run_explain.py` passed
- frontend type-check passed:
  - `cd frontend/wardo-hub-main; npx tsc -p tsconfig.json --noEmit`

## Legacy Upload Path Fix - 2026-02-23
1. Root cause found for missing wardrobe images:
- older records referenced `/uploads/<filename>` that originally lived in legacy `backend/uploads`.
- current server serves `/uploads` from `runtime/uploads/persistent`.
- mismatch caused older image paths to fail.

2. Applied backend startup migration for backward compatibility:
- updated `backend/app.js`
- added `migrateLegacyUploads()`:
  - scans `backend/uploads`
  - copies missing files into `runtime/uploads/persistent`
  - logs migrated file count on startup
- keeps current canonical serving path unchanged (`/uploads` -> `runtime/uploads/persistent`).

3. Validation for this pass:
- `node --check backend/app.js` passed
- file presence check confirmed legacy names now exist under `runtime/uploads/persistent`
- live HTTP check passed:
  - `HEAD http://localhost:8001/uploads/1771247903838_img_00000014.jpg` -> `200`

## Explanation Loading Overlay Pass - 2026-02-23
1. Added dedicated loading overlays for deferred explanation generation:
- updated `frontend/wardo-hub-main/src/pages/MixMatch.tsx`
- updated `frontend/wardo-hub-main/src/pages/GlowUp.tsx`
- behavior:
  - first loading overlay still covers upload + scoring phase
  - second overlay now appears after score/recommendation is ready while AI explanation is being generated
  - includes 3-dot animation and user-facing message to avoid confusion

2. State handling:
- introduced `isGeneratingExplanation` state on both pages
- toggled true before `/explain` request and false in `finally`

3. Validation for this pass:
- frontend type-check passed:
  - `cd frontend/wardo-hub-main; npx tsc -p tsconfig.json --noEmit`

## GlowUp Timeout Stabilization Pass - 2026-02-24
1. Root cause identified:
- `/recommend` cold-start can exceed 5-minute process timeout when retrieval caches are missing (notably `tops` embeddings), causing backend 500.

2. Backend timeout and workload tuning applied:
- updated `backend/utils/pipeline.js`
- changes:
  - added recommendation-specific timeout override:
    - `RECOMMEND_TIMEOUT_MS` default `900000` (15 min)
  - kept global timeout unchanged for other scripts
  - reduced API recommendation default shortlist:
    - `DEFAULT_RECOMMEND_SHORTLIST_K`: `150 -> 50`
- effect:
  - faster recommend calls after cache warmup
  - lower chance of timeout during normal operation

3. Better timeout error response:
- updated `backend/controllers/inference.js`
- recommend timeout now returns `503` with code:
  - `RECOMMEND_TIMEOUT`
  - message explains likely first-run cache warmup and asks retry in 1-2 minutes.

4. Cache warmup executed:
- prewarmed missing `tops` embedding cache via CLI recommend run (`mode=bottom2top`)
- verified both caches now exist:
  - `embeddings_bottoms_resnet18_d256_s224_train-valid_w7648a4976023.npz`
  - `embeddings_tops_resnet18_d256_s224_train-valid_w7648a4976023.npz`

5. Validation for this pass:
- `node --check backend/utils/pipeline.js` passed
- `node --check backend/controllers/inference.js` passed
- smoke timings:
  - warmup build run completed successfully (~216s)
  - cached recommend run (`shortlist_k=50`) completed successfully (~10s)

## Non-Blocking Explanation UX Pass - 2026-02-24
1. Removed blocking explanation overlays from both pages:
- updated `frontend/wardo-hub-main/src/pages/MixMatch.tsx`
- updated `frontend/wardo-hub-main/src/pages/GlowUp.tsx`
- explanation generation no longer covers the entire screen after results are ready.

2. Added inline animated explanation-progress indicators:
- inside each `Style Explanation` card, when `isGeneratingExplanation=true`:
  - animated dot row
  - short "AI stylist is writing your explanation..." status
  - skeleton placeholder lines when explanation text has not arrived yet
- badge now reflects generation state immediately (`Generating...`) while keeping results visible.

3. Validation for this pass:
- frontend type-check passed:
  - `cd frontend/wardo-hub-main; npx tsc -p tsconfig.json --noEmit`

## GlowUp Hybrid + Latency Pass - 2026-02-23
1. Added wardrobe-first hybrid recommendation path:
- updated `backend/controllers/inference.js`
- `/recommend` now:
  - resolves authenticated user's wardrobe items
  - builds local candidate list from Mongo records (`clothing`)
  - runs hybrid ranking when wardrobe candidates exist
  - falls back to catalog-only ranking when no wardrobe candidates are available

2. Added hybrid pipeline bridge in backend utility:
- updated `backend/utils/pipeline.js`
- new runner: `runHybridRecommendation(...)` calling:
  - `services/pipeline/scripts/run_recommend_hybrid.py`
- wardrobe candidates are written to temp JSON and cleaned up after run.

3. Improved recommendation latency defaults:
- updated `backend/utils/pipeline.js`
- recommend defaults changed to:
  - `top_k: 3` (was 5)
  - `shortlist_k: 40` (was 50)
  - shortlist clamp minimum reduced to 10 for faster low-latency runs

4. Added image-source URLs in recommend script outputs:
- updated `services/pipeline/scripts/run_recommend.py`
- each returned row now includes:
  - `source: "polyvore"`
  - `image_url: "/catalog-images/<id>.jpg"`

5. Hybrid script reliability improvements:
- updated `services/pipeline/scripts/run_recommend_hybrid.py`
- added explicit NumPy import at module level
- wardrobe candidate ingestion now defaults missing image URLs to:
  - `/uploads/<filename>`

6. Backend static serving for catalog images:
- updated `backend/utils/paths.js` with `POLYVORE_IMAGES_DIR`
- updated `backend/app.js` to serve:
  - `/catalog-images` -> `assets/data/polyvore_outfits/images`

7. GlowUp frontend now renders top-3 recommendation cards with images:
- updated `frontend/wardo-hub-main/src/pages/GlowUp.tsx`
- changes:
  - sends `top_k=3`, `shortlist_k=40` in request
  - displays top-3 cards with score, source badge (`Wardrobe`/`Catalog`), and image preview
  - resolves images from `image_url` with safe fallback mapping

## Auto-Detect Crop Direction Pass - 2026-02-23
1. Improved top-vs-bottom mode detection for recommend flows:
- updated `services/pipeline/scripts/run_recommend.py`
- updated `services/pipeline/scripts/run_recommend_hybrid.py`
- behavior in `mode=auto`:
  - trust direct semantic detection only when confidence is strong
  - when ambiguous, use YOLO visibility signals (`upper_body`/`mid_lower`) to choose direction
  - for full-body/uncertain cases, compare category confidence on top-focused vs bottom-focused crops and choose higher-confidence direction
  - fallback to previous semantic/default only when crop signals remain inconclusive

2. Why this fix:
- makes uploaded-top -> recommend-bottom and uploaded-bottom -> recommend-top more reliable, especially for noisy/full-body photos.

3. Validation for this pass:
- `python -m py_compile services/pipeline/scripts/run_recommend.py services/pipeline/scripts/run_recommend_hybrid.py` passed
- `python services/pipeline/scripts/run_recommend.py --help` passed
- `python services/pipeline/scripts/run_recommend_hybrid.py --help` passed

## GlowUp Label Fallback + Fast Mode Pass - 2026-02-23
1. Removed `Unknown` label fallback in GlowUp result badges:
- updated `frontend/wardo-hub-main/src/pages/GlowUp.tsx`
- labels now fall back to threshold-based score bands when `details.label` is missing:
  - `Excellent Match`, `Good Match`, `Borderline Acceptable`, `Weak Match`, `Mismatch`
- applied to both:
  - top recommendation summary badge
  - top-3 match cards

2. Added low-latency recommend fast mode end-to-end:
- frontend request now sends:
  - `fast_mode=1`
  - `shortlist_k=25`
- backend updated:
  - `backend/controllers/inference.js` parses `fast_mode` (default true for GlowUp path)
  - `backend/utils/pipeline.js` passes `--fast` to recommend scripts
  - backend default shortlist lowered to `25`
- pipeline scripts updated:
  - `services/pipeline/scripts/run_recommend.py`
  - `services/pipeline/scripts/run_recommend_hybrid.py`
  - new `--fast` flag:
    - disables pattern model inference (`pattern_predictor = None`) to reduce heavy per-candidate compute
    - caps effective shortlist to max `25`

3. Validation for this pass:
- `node --check backend/controllers/inference.js` passed
- `node --check backend/utils/pipeline.js` passed
- `python -m py_compile services/pipeline/scripts/run_recommend.py services/pipeline/scripts/run_recommend_hybrid.py` passed
- `python services/pipeline/scripts/run_recommend.py --help` passed (includes `--fast`)
- `python services/pipeline/scripts/run_recommend_hybrid.py --help` passed (includes `--fast`)
- frontend type-check passed:
  - `cd frontend/wardo-hub-main; npx tsc -p tsconfig.json --noEmit`

## Wardrobe Feature Persistence Pass - 2026-02-23
1. Added feature extraction script for single wardrobe item:
- new file: `services/pipeline/scripts/run_extract_item_features.py`
- outputs reusable per-item features:
  - semantic/category (+ confidence/source)
  - color palette + primary color
  - brightness/contrast/saturation metrics
  - pattern payload (model output or text heuristic fallback)
  - mask and autocrop metadata

2. Wired extraction into backend pipeline utility:
- updated `backend/utils/pipeline.js`
- added `runWardrobeFeatureExtraction(...)` that executes:
  - `run_extract_item_features.py`

3. Persisted extracted features in Mongo on wardrobe upload:
- updated `backend/controllers/wardrobe.js`
- `POST /wardrobe` now stores under `details`:
  - `features` (extracted payload)
  - `features_status` (`ok` / `error`)
- `features_error` when extraction fails (upload still succeeds)

4. Hybrid recommend fast-mode now consumes stored wardrobe features for prefiltering:
- updated `backend/controllers/inference.js` to forward `details.features` into wardrobe candidate payload.
- updated `services/pipeline/scripts/run_recommend_hybrid.py`:
  - in `--fast` mode, before heavy scoring, compute quick compatibility (color + brightness + pattern heuristic) using:
    - query visual/pattern extracted once
    - stored wardrobe features from Mongo
  - keep only top prefiltered wardrobe candidates (`24`) for heavy model scoring.
- effect:
  - reduces per-request candidate compute cost as wardrobe grows.

5. Storage decision:
- persistent reusable wardrobe features are now stored in Mongo (inside wardrobe document details)
- runtime script/cache artifacts remain on disk under `runtime/cache`

6. Validation for this pass:
- `node --check backend/controllers/wardrobe.js` passed
- `node --check backend/utils/pipeline.js` passed
- `node --check backend/controllers/inference.js` passed
- `python -m py_compile services/pipeline/scripts/run_extract_item_features.py` passed
- `python -m py_compile services/pipeline/scripts/run_recommend_hybrid.py services/pipeline/scripts/run_extract_item_features.py` passed
- `python services/pipeline/scripts/run_recommend_hybrid.py --help` passed
- `python services/pipeline/scripts/run_extract_item_features.py --help` passed
- frontend type-check passed:
  - `cd frontend/wardo-hub-main; npx tsc -p tsconfig.json --noEmit`

## Wardrobe Backfill Endpoint Pass - 2026-02-23
1. Added authenticated endpoint to backfill existing wardrobe features:
- updated `backend/route.js`
- new route:
  - `POST /wardrobe/backfill-features`

2. Implemented backfill controller:
- updated `backend/controllers/wardrobe.js`
- new handler: `backfillWardrobeFeatures`
- behavior:
  - scans current user wardrobe items (newest first)
  - default `limit=80` (configurable via body/query; clamped `1..300`)
  - default `force=false`
  - skips items that already have `details.features` with `features_status=ok` unless `force=true`
  - resolves upload image under `runtime/uploads/persistent`
  - runs pipeline extraction per item and stores into Mongo `details.features`
  - stores per-item status:
    - `features_status=ok` on success
    - `features_status=error` + `features_error` on failure/missing files
  - returns summary counters and capped failure list

3. Validation for this pass:
- `node --check backend/controllers/wardrobe.js` passed
- `node --check backend/route.js` passed
- `node --check backend/controllers/inference.js` passed
- `node --check backend/utils/pipeline.js` passed

## GlowUp Polyvore Crop Preview Pass - 2026-02-23
1. Added YOLO-cropped preview URLs for recommended Polyvore items:
- updated `services/pipeline/scripts/run_recommend.py`
- updated `services/pipeline/scripts/run_recommend_hybrid.py`
- behavior:
  - for each Polyvore recommendation row, run autocrop on candidate image using candidate semantic (`tops` / `bottoms`)
  - if crop is applied, response `image_url` now points to:
    - `/pipeline-autocrop/<crop_hash>.jpg`
  - fallback remains original catalog image URL if crop is unavailable
  - details now include:
    - `candidate_autocrop_preview` metadata (`applied`, `reason`, `body_visibility`, `confidence`, `crop_box`)

2. Exposed autocrop cache directory as static backend route:
- updated `backend/utils/paths.js`:
  - added `PIPELINE_CACHE_DIR`, `PIPELINE_AUTOCROP_DIR`
- updated `backend/app.js`:
  - added static route:
    - `/pipeline-autocrop` -> `runtime/cache/pipeline/autocrop`

3. Validation for this pass:
- `node --check backend/utils/paths.js` passed
- `node --check backend/app.js` passed
- `python -m py_compile services/pipeline/scripts/run_recommend.py services/pipeline/scripts/run_recommend_hybrid.py` passed
- `python services/pipeline/scripts/run_recommend.py --help` passed
- `python services/pipeline/scripts/run_recommend_hybrid.py --help` passed
- frontend type-check passed:
  - `cd frontend/wardo-hub-main; npx tsc -p tsconfig.json --noEmit`

## GlowUp Crop-Only Enforcement Pass - 2026-02-23
1. Fixed static route reliability for crop previews:
- updated `backend/app.js`
- `runtime/cache/pipeline/autocrop` is now always created on startup and route is always mounted:
  - `/pipeline-autocrop` -> `runtime/cache/pipeline/autocrop`

2. Enforced crop-only Polyvore previews (no full-body fallback):
- updated `services/pipeline/scripts/run_recommend.py`
- updated `services/pipeline/scripts/run_recommend_hybrid.py`
- behavior for Polyvore rows:
  - primary: YOLO crop preview
  - fallback when YOLO fails: semantic ratio crop (`tops` upper split / `bottoms` lower split) written to autocrop cache
  - if both fail: empty `image_url` (frontend shows unavailable) instead of full catalog image
- additional metadata retained in details:
  - `candidate_autocrop_preview`
  - `candidate_catalog_image_url` (for diagnostics only)

3. Frontend no longer falls back Polyvore rows to full catalog image:
- updated `frontend/wardo-hub-main/src/pages/GlowUp.tsx`
- if source is `polyvore` and `image_url` is empty, preview is shown as unavailable instead of loading `/catalog-images/<id>.jpg`.

4. Validation for this pass:
- `node --check backend/app.js` passed
- `python -m py_compile services/pipeline/scripts/run_recommend.py services/pipeline/scripts/run_recommend_hybrid.py` passed
- `python services/pipeline/scripts/run_recommend.py --help` passed
- `python services/pipeline/scripts/run_recommend_hybrid.py --help` passed
- frontend type-check passed:
  - `cd frontend/wardo-hub-main; npx tsc -p tsconfig.json --noEmit`
- live `/recommend` smoke verified:
  - Polyvore rows returned `/pipeline-autocrop/*.jpg`
  - fallback reason observed: `semantic_fallback_after_empty_boxes`

## Wardrobe Pattern Re-Enable Pass - 2026-02-23
1. Re-enabled pattern contribution for wardrobe matching in fast mode:
- updated `services/pipeline/scripts/run_recommend_hybrid.py`
- behavior:
  - query pattern is computed once (model if available; text-derived fallback otherwise)
  - wardrobe candidate pattern uses cached Mongo features (`details.features.pattern`) via reconstructed `PatternPrediction`
  - if cached candidate pattern is missing, text-derived fallback is used
  - this restores pattern-aware scoring for wardrobe rows without heavy per-candidate pattern inference

2. Kept Polyvore fallback fast:
- in `--fast` mode, pattern model remains disabled only during Polyvore `pipe.rank(...)` rerank stage
- this keeps latency bounded while preserving wardrobe pattern quality

3. Validation for this pass:
- `python -m py_compile services/pipeline/scripts/run_recommend_hybrid.py` passed
- `python services/pipeline/scripts/run_recommend_hybrid.py --help` passed
- live `/recommend` smoke verified:
  - wardrobe row returned `pattern_source: image_model`
  - pattern labels populated (`top_pattern_label`, `bottom_pattern_label`)
  - polyvore rows remained `pattern_source: text_heuristic` in fast mode

## GlowUp Source-Priority UI Pass - 2026-02-23
1. Refined Top-3 results UX to clearly separate personal wardrobe vs dataset fallback:
- updated `frontend/wardo-hub-main/src/pages/GlowUp.tsx`
- changes:
  - split results into 2 sections:
    - `From Your Wardrobe` (priority)
    - `Style References (Polyvore Dataset)` (fallback/inspiration)
  - added source summary banner:
    - `<wardrobe_count> from your wardrobe, <polyvore_count> pulled from Polyvore fallback`
  - added fallback context copy for Polyvore references:
    - reference-only messaging (not currently in wardrobe)

2. Polyvore reference section is now secondary/collapsible:
- defaults:
  - collapsed when wardrobe matches exist
  - auto-expanded when no wardrobe matches are available
- added explicit toggle button:
  - `Show References` / `Hide References`

3. Wardrobe-first emphasis preserved:
- top recommendation summary now prioritizes first wardrobe match when available.

4. Validation for this pass:
- frontend type-check passed:
  - `cd frontend/wardo-hub-main; npx tsc -p tsconfig.json --noEmit`

## Color Top-3 Weighted Palette Pass - 2026-02-23
1. Upgraded color harmony from single dominant color to top-3 weighted palette matching:
- updated `services/pipeline/src/outfit_pipeline/features.py`
- new approach:
  - keeps existing per-pair color harmony rules (neutral handling, hue-distance bands, value-diff bonus)
  - evaluates top-3 palette colors with normalized cluster weights
  - computes bidirectional best-match score:
    - top->bottom and bottom->top weighted matching
  - blends with a small dominant-color anchor for backward stability

2. Expected impact:
- better handling of multi-color garments (less brittle than top-1 only)
- improved robustness when primary color cluster alone is misleading

3. Validation for this pass:
- `python -m py_compile services/pipeline/src/outfit_pipeline/features.py` passed
- `python services/pipeline/scripts/run_pair.py --help` passed
- `python services/pipeline/scripts/run_recommend.py --help` passed
- `python services/pipeline/scripts/run_recommend_hybrid.py --help` passed
